import React from 'react';
import { Link } from 'react-router-dom';

const navBlock = () => {
    return (
        <div>Navigation
            <Link to="/">Back</Link>
        </div>
    );
};

export default navBlock;